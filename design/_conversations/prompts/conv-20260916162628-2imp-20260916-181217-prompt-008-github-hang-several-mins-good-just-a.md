# Prompt 008

## Metadata

- Prompt ID: prompt-20260916181217-8gp2dy
- Conversation ID: conv-20260916162628-2imp
- Timestamp: 2026-09-16T17:12:17.551Z
- Sequence Number: 8
- Source URL: https://m365.cloud.microsoft/chat/conversation/6ebae526-f9e8-45bf-9af3-dff9279d7bae

## Prompt

github hang (several mins ) :​‌Good — just a formatter reflow, no substantive change. Let's quickly confirm the full suite is still green before starting Step 18.ThinkingRan TZ=UTC npx vitest run 2>&1 | tail -20With the baseline confirmed green, I'm moving to Step 18: building the ports query endpoints, location search, and map projection. I'm reviewing the port schema fields and recalling that port codes should use case-sensitive exact matching based on earlier notes.For country codes I'm leaning toward case-insensitive matching since ISO codes are canonically uppercase but I want to be forgiving. I'm defining search behavior across name, code, and countryCode fields, plus radius-based search requiring latitude, longitude, and radiusKm together, with validated ranges and a max radius of 1000km using Haversine distance with inclusive boundaries. I'm also planning bounding-box support for map queries, keeping it simple by rejecting antimeridian-crossing boxes for now.​‌I'm working through view parameters and pagination rules — ports collection endpoints support view and pagination normally, but map/ports is stripped down to just bbox with no pagination or view support, rejecting any unrecognized params. I'm also figuring out ETag handling, since map/ports is a derived, non-persisted dataset that doesn't live in the store like the regular ports collection does, so I'll need a separate caching mechanism for it.Checking the store logic, I realize getCollection('map-ports') won't throw since map-ports is a recognized dataset type, but it'll just return undefined since nothing ever populates it there. So instead of using the generic collection-query-service, the map-ports controller needs to read the live ports collection directly and derive GeoJSON features from it on the fly.I'll build a small dedicated module for this: a function that transforms the ports collection plus metadata into a GeoJSON FeatureCollection, paired with a separate lightweight query path just for map-ports rather than routing it through the generic dataset engine.​‌That path pulls the ports collection and metadata straight from the store, derives features, optionally applies a shared bbox-filter function (reusable later for Step 20), and computes a deterministic ETag from metadata plus the bbox param before returning the response. I'm weighing whether it's still cleaner to keep this separate versus reusing the generic engine with a custom "prepareRecords" hook that maps ports into features — since exact-filter and text-search machinery aren't needed here anyway.I realize the format mismatch is real: the generic loader expects .features for GeoJSON but the ports collection stores .items, so I can't just flip the format flag. The simplest, most correct route is to bypass the generic engine entirely for map-ports and write a small dedicated pure module plus service, matching the guidance that map-ports should be derived rather than forced through the engine.I'll create a shared bounding-box query module with strict parsing and validation (rejecting malformed coordinates and antimeridian cases as unsupported) plus a point-in-box check, and a location-search module built around the haversine distance formula for later reference-data queries.​‌For the radius search params, I'm realizing the standard customFilters mechanism validates each query param independently, but latitude/longitude/radiusKm need cross-field validation—if any one is present, all three must be. I'll need to add a dedicated check for that "all-or-nothing" constraint since the parser doesn't support it natively.Simpler approach: key the customFilter on radiusKm itself, since its presence signals a radius search. Its parse(raw, rawQuery) reads latitude, longitude, and radiusKm together from rawQuery, validates all three exist, and returns them bundled as one parsed value. Then the predicate has everything it needs to compute distance and compare against the radius.​‌But I realize latitude/longitude also need to be marked as recognized keys, otherwise the generic unsupported-parameter check will reject them even though only radiusKm's filter is doing the actual work — I need all three declared as recognized without latitude/longitude triggering their own separate predicates.Rather than modify the shared engine, I'll add latitude and longitude as customFilters with no-op predicates that always return true, but with their own parse logic that validates the numeric ranges and requires their companion params to be present. All three get merged into projectionContext via customFilterValues, but only radiusKm's predicate actually performs filtering — latitude/longitude just act as validation gates.Actually, I realize predicate(record, value) only receives the parsed radiusKm value alone, not the lat/lon needed for distance calc, so those need to be merged in somehow. The fix: have radiusKm's parse function return a composite object with latitude, longitude, and radiusKm together, so its predicate can do the full distance calculation using all three values, while separate latitude/longitude customFilters exist purely so the query params are recognized rather than rejected as unsupported.​‌Writing the shared validation helper...Wiring up custom filters...But the predicate only receives the single scalar value for its own key, not the other two coordinates, so radiusKm alone can't filter distance properly. I realize the recognized-key set is literally derived from the customFilters keys as query param names, so I can't introduce a synthetic combined key without it also being an accepted param name — I need to find a way to let one filter's predicate access sibling parsed values instead.That's fine since ports mobile projector doesn't need location context. Simplifying pragmatically: I'll make all three customFilters' parse return the same composite validated triple object, with only radiusKm's predicate doing real filtering whileThinking​‌
