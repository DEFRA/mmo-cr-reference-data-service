# Prompt 006

## Metadata

- Prompt ID: prompt-20260916094516-6gq622
- Conversation ID: conv-20260916083321-tp10
- Timestamp: 2026-09-16T08:45:16.003Z
- Sequence Number: 6
- Source URL: https://m365.cloud.microsoft/chat/conversation/6ebae526-f9e8-45bf-9af3-dff9279d7bae

## Prompt

fron github :The 6 unresolved points — exact schema/code stateStatistical-area parentCode — map-statistical-areas.js: parentCode: Joi.string().min(1).allow(null).optional(). No format beyond "non-empty string or null" is enforced, and no relationship-resolution logic exists. The existing valid fixture (map-statistical-areas.json) has one feature with "parentCode": "27D8", but no feature with properties.code === "27D8" exists in that same file.→ Should Step 09 require parentCode to resolve to another feature's properties.code within the same uploaded collection when present, or leave it structurally-only (unenforced)?Gear characteristic dataType — gears.js: dataType: Joi.string().min(1).required(). Any non-empty string currently passes. The only value appearing anywhere in the repo (schema tests/fixtures) is "number". No enum or constants list exists in code or in any design doc I can find.→ What is the exact, complete list of approved dataType values? (I will not invent additional values beyond what you confirm.)Business-code duplicate comparison — no dataset validator exists yet, so nothing is decided in code. Only the reusable utility exists, defaulting to case-sensitive.→ For each identifier family — vessel CFR/UVI/MMSI/IRCS/externalMark/registrationNumber, gear/category/characteristic codes, port codes, FAO codes, statistical-area codes — should duplicate detection be case-sensitive (exact) or case-insensitive? (Can be a single uniform answer or per-family.)​‌Species nested-name GUID uniqueness — species.js: commonNames[].id and localNames[].id are each just guidSchema.required() — no scoping rule is stated anywhere.→ Must commonNames[].id / localNames[].id be unique: (a) only within each nested array, (b) within each species record across both name types, (c) collection-wide per name type, or (d) collection-wide across both name types combined?Vessel-length applicability — exact current schema, gears.js: applicableCharacteristics[].vesselLengthApplicability: Joi.array().items(Joi.string().min(1)).optional() — a free-form array of non-empty strings, no enum. The only fixture example uses ["under-10m", "10-to-12m"]; no third value appears anywhere in code.→ Is there an approved, exact list of canonical band values (e.g. is a third "over 12m" value spelled a specific way), and where is that list authoritative — should I only ask you, since nothing in the repo defines it?Vessel length non-negativity vs. structural schema conflict — vessels.js: lengthOverallMetres: Joi.number().strict().positive().required(). positive() already rejects 0 at the structural (Step 04) layer. The Step 09 prompt asks for a business rule that only rejects negative values (implying 0 should be structurally acceptable).→ Should the Step 04 schema be relaxed to min(0) so 0 is structurally valid (making the Step 09 rule meaningful), or should the Step 09 "non-negative" rule be treated as already satisfied/redundant given positive() is stricter and not to be changed?I'm holding all Step 09 planning and implementation until you answer these.​‌
