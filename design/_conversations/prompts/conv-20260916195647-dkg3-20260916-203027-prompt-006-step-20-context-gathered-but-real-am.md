# Prompt 006

## Metadata

- Prompt ID: prompt-20260916203027-z4ihhl
- Conversation ID: conv-20260916195647-dkg3
- Timestamp: 2026-09-16T19:30:27.648Z
- Sequence Number: 6
- Source URL: https://m365.cloud.microsoft/chat/conversation/6ebae526-f9e8-45bf-9af3-dff9279d7bae

## Prompt

## Step 20 — Context gathered, but real ambiguities found before implementingFollowing the same discipline from Step 19: I inspected the master plan (Step 20 section, `reference-data-service-implementation-plan.md` lines 1242–1281), the canonical schemas (`map-land.js`, `map-statistical-areas.js`), the existing `bounding-box.js` (built in Step 18, reusable as-is for the point/bbox parsing part), and confirmed **no polygon-intersection utility exists anywhere** in the repo (only structural ring-closure/coordinate-range validation in `geojson-validation.js`, which is a different concern).### Confirmed / reused — no need to ask| Item | Resolution | Basis ||---|---|---|| Bbox syntax (`minLon,minLat,maxLon,maxLat`), inclusive boundaries, antimeridian rejection | Reuse `bounding-box.js` unchanged | Explicit Step 18 owner decision, built for this reuse || `view=canonical/mobile` on map endpoints | Not supported (GeoJSON-only, same as `map/ports`) | Established Step 18 precedent || Pagination on map layers | Not applied (full layer, bbox is the filter) | Established Step 18 precedent || `map/land` route shape | `GET /api/v1/reference-data/map/land` only — **no** `/map/land/{id}` | Master plan explicitly lists only 3 routes total, none for individual land features || Filters applicable to `map-land` | `query` (name) + `bbox` only — **no** `code`/`parentCode` | `mapLandFeaturePropertiesSchema` has only `{id, name}` — there is no `code` property to filter on; this is a schema fact, not an inferred policy || Filters applicable to `map-statistical-areas` | `query`, `code`, `parentCode`, `bbox` | `statisticalAreaPropertiesSchema` has `{id, code, name, areaType, parentCode, parentName, areaKm2, centroid}` || Active/inactive filtering | Not applicable to either map dataset | Neither schema has an `active` property || `{id}` route | Statistical areas only, GUID-only (never `code`) | Master plan scope; consistent with every prior step |### Genuine open ambiguities — need your decision before I write any code**A. `code` (area-code) filter casing** — exact match confirmed by scope, but case-sensitive or case-insensitive is undefined anywhere.**B. `parentCode` filter casing** — same question.**C. General text-search field list for `map-statistical-areas`** — the prompt lists "may include" name/code/areaType/parentCode/parentName without locking a final set.- Smallest option: `name` + `code` only (mirrors land's single-field simplicity).- Broader option: `name`, `code`, `areaType`, `parentCode`, `parentName` (everything text-like).**D. Polygon/MultiPolygon bounding-box intersection semantics — the significant one.** The prompt explicitly requires "true intersection" (not just centroid- or vertex-only checks) and explicitly says a new geometry library must not be added without approval. Options:1. **True geometric intersection, pure JS, no new dependency** — implemented via: (i) any polygon vertex inside the bbox, OR (ii) any bbox corner inside the polygon (ray-casting point-in-polygon), OR (iii) any polygon edge crosses any bbox edge (segment intersection). This correctly handles all cases the prompt calls out (bbox entirely inside polygon, polygon entirely inside bbox, partial overlap, boundary contact) without a dependency.2. **Approximate: feature's own axis-aligned bounding box vs the query bbox** — much simpler, but not "true intersection" (would treat any shape as its envelope), likely non-compliant with the prompt's explicit warning.3. **Add a geometry library** (e.g. `@turf/boolean-intersects`) — most robust/tested, but requires your explicit approval per the prompt's own dependency-approval rule (security/size/licence/maintenance review first).My recommendation: **Option 1** (pure JS, no new dependency) — it satisfies "true intersection" correctness for polygon/bbox cases without adding a dependency, and is a bounded, testable algorithm. Option 3 would be more defensible long-term if you're open to a small, well-known, actively-maintained geometry dependency — your call.**E. Malformed persisted geometry encountered during a query** — fail the whole request, or skip/exclude just the affected feature? The prompt lists this as a required error case but doesn't state which.I won't guess any of these. Please tell me:1. Area-code casing (case-sensitive / case-insensitive)?2. Parent-code casing (case-sensitive / case-insensitive)?3. Statistical-area search fields — narrow (`name`+`code`) or broad (`name`,`code`,`areaType`,`parentCode`,`parentName`)?4. Polygon intersection approach — Option 1 (pure JS) or Option 3 (add a geometry dependency, subject to your approval)?5. Malformed geometry — fail whole request, or exclude the one feature?I'll save the Step 20 plan and begin implementation as soon as you confirm these.Created 3 todos​‌
