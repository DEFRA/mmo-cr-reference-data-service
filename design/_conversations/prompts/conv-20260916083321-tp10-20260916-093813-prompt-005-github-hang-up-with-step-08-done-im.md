# Prompt 005

## Metadata

- Prompt ID: prompt-20260916093813-xdxgqt
- Conversation ID: conv-20260916083321-tp10
- Timestamp: 2026-09-16T08:38:13.751Z
- Sequence Number: 5
- Source URL: https://m365.cloud.microsoft/chat/conversation/6ebae526-f9e8-45bf-9af3-dff9279d7bae

## Prompt

github hang up : ​‌With Step 08 done, I'm moving to Step 09: implementing dataset-specific validation, and I'll write its plan file capturing the clarified ambiguities—statistical-area parentCode as optional/informational without collection enforcement, and gear characteristic dataType limited to number, string, or boolean.I'm also settling vessel-length bands (under-10m, 10-to-12m, over-12m), case-insensitive business-code duplicate checks, and collection-wide GUID uniqueness for species nested names across separate global sets for commonNames and localNames.Now I'm sketching the file structure under validation/datasets, with separate validator files per dataset plus constant lists for gear characteristic types and vessel length bands, and a registration file to wire everything into the registry—thinking through how to avoid circular import issues with the existing placeholder-based registry by having each dataset file export its own validator function.I'm planning a datasets/index.js that registers all six validators, imported for side effects from the main validation index, keeping the registry as the single source of truth without switch statements or controller changes. Each validator will follow a consistent signature returning a ValidationResult via the issue collector, reusing shared utilities like duplicate detection and reference indexing.Now I'm working out the vessels validator rules: requiring at least one natural identifier (cfr, uvi, mmsi, ircs, externalMark, or registrationNumber) per record, and enforcing case-insensitive uniqueness per identifier type.For vessel length, I'm noticing a tension between the schema, which already uses .positive() and thus excludes zero, and the business rule prompt, which frames the check as "not negative" and implies zero might be structurally permitted. Since the schema decision already locks out zero and negative values, I'll add a light defensive check but treat the schema constraint as the authoritative source here.Since the master plan says schemas are locked, I'll skip building a redundant business rule for vessel length and instead document that the structural schema already guarantees positivity. For active date ranges, I'll enforce that activeTo doesn't precede activeFrom using isValidDateOrder, then move on to the gears validator, starting with uniqueness checks across gear codes, category codes, and characteristic codes.Then I'm working through the reference resolution rules: category and characteristic IDs must resolve properly, data types must match the supported set, numeric min/max ranges must be consistent, and vessel-length applicability arrays must be non-empty and use valid band values.Considering​‌
