# Prompt 010

## Metadata

- Prompt ID: prompt-20260916192017-fozlc4
- Conversation ID: conv-20260916162628-2imp
- Timestamp: 2026-09-16T18:20:17.924Z
- Sequence Number: 10
- Source URL: https://m365.cloud.microsoft/chat/conversation/6ebae526-f9e8-45bf-9af3-dff9279d7bae

## Prompt

## Working-tree recovery report**Git status:** clean relative to Step 19 — `git status --short` / `git diff --name-only` show only the pre-existing Step 14–18 modified/untracked files (README, `router.js`, `controller/index.js`, `query/index.js`, `reference-data-components.test.js`, and all the already-completed Step 14–18 source/test/plan files). **No Step 19 files exist anywhere** — no `species-query-configuration.js`, no `species-mobile-projector.js`, no `species-name-resolver.js`, no `routes/species.js`, and no saved `Step 19-implement-species-query-endpoints-and-name-resolution-plan.md`. The only Step 19 artifacts present are the prompt itself (`step-19-implement-species-query-endpoints-and-name-resolution.md`/`.meta.md`). **The prior operation stopped before any Step 19 implementation began** — there is nothing to preserve, edit, or revert.**No background process is running** (no vitest/npm/node test runner active).## Locating the "approved API definition"There is **no separate API-design document** in this repository distinct from:1. `reference-data-service-implementation-plan.md` (the locked master plan, Step 19 section, lines 1191–1230), and2. `step-19-implement-species-query-endpoints-and-name-resolution.md` (the detailed prompt itself).The master plan's entire Step 19 definition is:> Implement `GET /api/v1/reference-data/species`, `GET /api/v1/reference-data/species/{id}`. Support filtering by: General text, FAO code, Scientific name, Country code, Language code. Support `Accept-Language`. Implement this mobile common-name resolution order: 1. Official local name matching requested language. 2. Common name matching requested country. 3. Official `en-GB` local name. 4. First available country common name. 5. Scientific name. 6. FAO code.That's it — it names _which_ filters must exist and gives the exact 6-step resolution order, but defines no casing/matching semantics for any filter, no general-text-search field list, and no country-code source for mobile resolution.The Step 19 prompt itself repeatedly refers to "the approved API design" for these exact details (e.g. "Use exact matching according to **approved casing semantics**", "Do not assume exact or partial matching without checking Step 15 and **the API design**") — but that document does not exist separately; the prompt's own Mandatory Ambiguity Rule (lines 49–74) lists these same items as things that must not be guessed.## API conformance matrix| # | Requirement | Source | Endpoint/param | Required behaviour | Current implementation | Change needed? | Ambiguity ||---|---|---|---|---|---|---|---|| 1 | Routes exist | Master plan L1197–1198; prompt L137–138 | `GET /species`, `GET /species/{id}` | Both routes implemented | Not implemented | Yes (net new) | None || 2 | Mobile name-resolution precedence | Master plan L1212–1218; prompt L154–161, 533–585 | mobile view | Exact 6-step order: official local (requested lang) → common name (requested country) → official en-GB local → first country common name → scientificName → faoCode | Not implemented | Yes (net new) | **None — explicitly defined, must be preserved exactly** || 3 | Canonical response completeness | Master plan L1223; prompt L455 | `view=canonical` | Retain all common/local names, FAO code, scientific name | Not implemented | Yes (net new) | None || 4 | FAO code is a business code, not the resource ID | Master plan L1225; prompt L331, 585 | `{id}` route, FAO filter | `{id}` is always GUID; FAO code never used as resource ID | Not implemented | Yes (net new) | None || 5 | General text search — field list | Prompt L614–631 | `?query=` | "may include" FAO code, scientific name, common names, local names — final list unresolved (L54–55) | N/A | **Blocked** | **Unresolved — no field list is confirmed anywhere** || 6 | FAO-code filter — matching/casing | Prompt L639–659 | `?faoCode=` | "exact matching according to approved casing semantics" — casing never defined (L51–52) | N/A | **Blocked** | **Unresolved — no casing rule exists in any approved source** || 7 | Scientific-name filter — exact vs partial | Prompt L660–673 | `?scientificName=` | "exact approved matching semantics" — never defined (L53) | N/A | **Blocked** | **Unresolved** || 8 | Country-code filter — casing | Prompt L679–699 | `?countryCode=` | "approved country-code matching semantics" — never defined (L62) | N/A | **Blocked** | **Unresolved** || 9 | Language-code filter — casing | Prompt L703–723 | `?languageCode=` | "exact approved BCP 47 matching semantics" — never defined (L61) | N/A | **Blocked** | **Unresolved** || 10 | Country context source for mobile resolution | Prompt L56–57, 679–699 | mobile view | Source (query param / header / actor context) not defined | N/A | **Blocked** | **Unresolved** || 11 | `Accept-Language` parsing depth | Prompt L58–66, 483–510 | request header | Single tag only, vs full weighted RFC parsing — not defined | N/A | **Blocked** | **Unresolved** || 12 | Filter combination (AND/OR) | Prompt L744–747 | multiple filters | Defers to "Step 15 semantics" — Step 15 defines AND for all filter types, so this one is _not_ ambiguous | N/A | Yes (net new, reuse existing AND) | None |I have **not** implemented items 5–11 using schema/duplicate-detection conventions, other-dataset precedent, or any other inference — per your instruction, these remain open.## Clarification needed before any Step 19 implementationFor each, please choose an option (or state another):**1. General text search fields (`?query=`)**- A) FAO code + scientific name + all common names + all local names- B) Only scientific name + common/local names (exclude FAO code, since it has its own exact filter)- C) Something else- _Effect_: A is broadest/most consumer-friendly; B keeps FAO code exact-only everywhere.**2. FAO-code filter (`?faoCode=`) casing**- A) Case-sensitive exact match- B) Case-insensitive exact match- _Effect_: affects whether `?faoCode=cod` matches a stored `"COD"`.**3. Scientific-name filter (`?scientificName=`) matching**- A) Exact match (case-sensitive)- B) Exact match (case-insensitive)- C) Partial/substring match (case-insensitive)- _Effect_: C is more consumer-friendly for multi-word Latin names; A/B require the exact stored string.**4. Country-code filter (`?countryCode=`) casing**- A) Case-sensitive exact- B) Case-insensitive exact- _Effect_: ISO country codes are conventionally uppercase; B is more forgiving of client input.**5. Language-code filter (`?languageCode=`) casing/matching**- A) Case-sensitive exact (BCP 47 tags are case-insensitive by spec, so this is unusual)- B) Case-insensitive exact- _Effect_: BCP 47 formally defines tags as case-insensitive; B aligns with that spec.**6. Country-context source for mobile name resolution (Rule 2)**- A) A `country` (or similarly named) query parameter, independent of `countryCode` filter- B) Reuse the `countryCode` filter value itself as the resolution context- C) Some other source (e.g. `Accept-Language` region subtag, e.g. `en-GB` → `GB`)- _Effect_: A/B change the query surface; C avoids adding a parameter but couples language and country resolution together, which the prompt says must stay independent "unless the API design explicitly states otherwise."**7. `Accept-Language` parsing depth**- A) Only the first valid language tag is used- B) Full RFC 4647/7231 weighted, ordered preference list is honoured- _Effect_: A is much simpler and matches most of this repo's existing "smallest safe default" pattern; B is materially more complex and the prompt explicitly warns not to claim "full support" without actually doing it.I will not proceed with any Step 19 code, plan file, or design decision until you answer these. Awaiting your input.Created 4 todos​‌
